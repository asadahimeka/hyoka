import http from '../api/http'

const auth = {
  loggedIn() {
    // http.post('/api/sso/login-info').then((userMsg) => {
    //   if (userMsg.data.status == 1) {

    //   } else {

    //   }
    // })
  }
}

export default auth
